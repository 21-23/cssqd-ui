import * as Actions from '../actions/#{name}-actions';

const initialState = null;

function #{camelCaseName}Reducer(state = initialState, action) {
    switch (action.type) {
        default: return state;
    }
}
